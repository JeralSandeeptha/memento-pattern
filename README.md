# Memento Pattern

Provide an ability to revert the object to it's previous state

Key Roles
Originator
The main object whose state changes (e.g., a text editor)
Memento
Stores a snapshot of the state
Caretaker
Manages saved states (undo history), but doesn’t modify them

```mermaid

```
